server:
  port: 9100

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

config:
  security:
    oauth:
      client:
        id: backend-specialization
        secret: backend-dh
      jwt:
        key: secret-code